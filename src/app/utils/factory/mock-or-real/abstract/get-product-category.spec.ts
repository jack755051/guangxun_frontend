import { GetProductCategory } from './get-product-category';

describe('GetProductCategory', () => {
  it('should create an instance', () => {
    expect(new GetProductCategory()).toBeTruthy();
  });
});
